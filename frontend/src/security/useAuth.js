// import Cookies from 'js-cookie';

import Cookies from "js-cookie";

export const useAuth = () => {
    const isAuthenticated = () => {
        return true; // TODO
    };

    return { isAuthenticated};
};
