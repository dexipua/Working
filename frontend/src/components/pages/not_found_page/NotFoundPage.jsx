import React from 'react';
import Typography from "@mui/material/Typography";

const NotFoundPage = () => {
    return (
        <Typography p={"20px"} variant={"h4"} color={'error'}>
            404 Page not found
        </Typography>
    );
};

export default NotFoundPage;